Compile binaries Reportgenerator and deploy

1) Run _builddeploy.bat (or _build.bat and _deploy.bat) - this prepares binaries in build folder
2) Run _release.bat - this creates deployment in release folder
3) Zip directory release

Deploy
6) Deploy (unzip) <zip>\release to ..\06_Tools\ReportTool\

Prerequisites:
.Net Core v2.1 on Windows or Linux